# Truecolor (24-bit) passthrough so nvim/rose-pine render exact colors
# instead of being downsampled to 256-color approximations.
set -g default-terminal "tmux-256color"
set -ga terminal-features ",*:RGB"
